[global]

time_stamping      software
network_transport  L2
delay_mechanism    E2E

# Make this clock preferable in BMCA.
priority1          100

# LinuxPTP 3.1.1 terminology:
# this port must never become a slave.
masterOnly         1
